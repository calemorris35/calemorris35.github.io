/*!
 * Sticky-kit
 * A jQuery plugin for making smart sticky elements
 *
 * Source: http://leafo.net/sticky-kit/
 */

$("#summer_character").stick_in_parent({
    // offset_top: 10,
    // offset_left: 100
});

$("#spring_character").stick_in_parent({
     offset_top: 10
});

$("#fall_character").stick_in_parent({
    // offset_top: 10
});

$("#winter_character").stick_in_parent({
    // offset_top: 10
});
